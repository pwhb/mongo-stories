export const create = (model: any, payload: any) => {
	const keys = Object.keys(model);

	console.log('payload', payload);
	console.log('keys', keys);
	// console.log('model.prototype', model.prototype);
};
